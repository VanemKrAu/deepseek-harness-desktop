registry=https://registry.npmjs.org/
# 说明：dshmarket（插件市场包）的 peerDependencies 只声明到 DSH 0.1.2-alpha.2，
# 与升级后的 0.1.5-rc.2 冲突（上游声明滞后）。安装时请用 `npm install --force`：
# --force 会忽略该冲突但**仍安装 peer 依赖**（DSH 各包间依赖大量使用 peer 声明，
# 若用 --legacy-peer-deps 会跳过 peer 安装，导致 dsh-jobs/dsh-settings 等缺失、
# 插件树加载失败）。
